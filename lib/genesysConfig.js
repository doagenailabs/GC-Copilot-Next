const baseConfig = {
    redirectUri: window.location.origin,
    appName: 'GCCopilotNext',
    defaultEnvironment: 'mypurecloud.ie',
    defaultLanguage: 'en-us'
};

window.SecureWindowManager.defineConfig(baseConfig);
